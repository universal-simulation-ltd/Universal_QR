<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- ⚠️ The last three items are one decision with
         `android.adjustMarginsForEdgeToEdge: 'auto'` in capacitor.config.ts, and
         neither half works alone. The Capacitor option pulls the web view out
         from under the status bar and the camera cutout (Android 15+ lays the
         window out edge-to-edge); the strips it leaves show the WINDOW
         background. DayNight takes that background and the status-bar icon
         colour from the SYSTEM theme, so without these a phone in dark mode
         would get a near-black strip with white icons above Universal QR's light
         navbar. Pinned light to match the app's default theme; see colors.xml
         for what that means when the app is set to Dark. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/unisimWindowBackground</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>